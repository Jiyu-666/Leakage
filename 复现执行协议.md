# 逐频各向异性模拟复现执行协议

## 1. 当前范围

第一阶段只复现论文《Detecting per-frequency anisotropy in gravitational background with Pulsar Timing Array》的受控模拟部分，不做 holodeck 种群实验。

目标顺序：

1. 在脉冲星对相关函数空间验证各向同性和已知天空图的前向模型。
2. 复现论文的逐频点源各向异性实验。
3. 在相同管线上加入 on-grid/off-grid CW。
4. 联合空间指纹与跨频泄漏，区分确定性 CW 和随机背景热点。

## 2. 论文已锁定的物理定义

论文采用稳态、高斯、无极化随机背景：

\[
\langle h_A^*(f,\Omega)h_{A'}(f',\Omega')\rangle
=\delta_{AA'}\delta(f-f')\delta^2(\Omega,\Omega')S_h(f,\Omega),
\]

并分解为

\[
S_h(f,\Omega)=H(f)P(f,\Omega).
\]

脉冲星对的角响应为

\[
\Gamma_{ab}(f)=\int d\Omega\,P(f,\Omega)
\sum_{A=+,\times}F_a^A(\Omega)F_b^A(\Omega).
\]

论文明确采用 Earth-term 近似，忽略含脉冲星距离和相位的指数项。各向同性输入必须恢复论文归一化下的 Hellings--Downs 相关。

计时残差模型为

\[
\delta t=M\epsilon+Fa+n_w,
\]

红噪声互协方差写为

\[
S_{ab}=\Gamma_{ab}F_a\phi F_b^T,
\qquad
\phi_{dd'}=\delta_{dd'}H(f_d)\Delta f.
\]

逐频相关量使用 Gersbach 等人的 PFOS 方法。天空功率采用 HEALPix 与平方根球谐表示，目标配置为 `Nside=8`、`lmax=6`。

## 3. 复现对象

### 3.1 基础闭环

- 各向同性图到 HD 曲线。
- 单像素点源到解析天线响应。
- 已知低阶球谐图的注入与恢复。
- `Nside=4,8,16` 的像素积分收敛。

### 3.2 论文点源实验

- 论文在信号生成处明确写 68 颗宽带脉冲星；NG15 标准 GWB 分析使用 67 颗，剔除观测不足 3 年的 J0614-3329。主复现保留 68 颗“论文逐字分支”和 67 颗“NG15 标准分支”，不得混写。
- 观测跨度 16.03 yr，频率网格 \(f_k=k/T\)。
- 48 个均匀天空像素；正文未公布索引。候选方案为 Nside=2 的 48 个像素中心投影到 Nside=8，并必须做旋转/替代均匀点集敏感性分析。
- 论文列出的 5 个频率为 `[1.98, 3.96, 5.94, 7.92, 9.90] nHz`。它们是 1.98 的整数倍，与 16.03 yr 的精确 \(k/T\)（首频 1.9768 nHz）并不完全一致；主分支采用列值，精确网格作为敏感性分支。
- \(C_1/C_0=0.3,0.4,0.5\)。
- 似然比统计量和逐像素上限统计量。
- 阈值只由独立 null 模拟确定。

论文图 4 的数值验收目标（每项分母为 48 个方向）：

| \(C_1/C_0\) | SNR：五频段成功率 | 像素上限：五频段成功率 |
|---|---|---|
| 0.3 | 6.25, 4.17, 0, 0, 0% | 45.83, 45.83, 25, 22.92, 20.83% |
| 0.4 | 18.75, 4.17, 0, 0, 0% | 45.83, 52.08, 35.42, 31.25, 16.67% |
| 0.5 | 31.25, 18.75, 2.08, 0, 0% | 58.33, 39.58, 31.25, 27.08, 25% |

五频段脉冲星对相关量标准差的论文报告中位数为
`[11.76, 11.95, 15.61, 20.83, 30.78]`。

### 3.3 CW 扩展

在不改变阵列、噪声和统计阈值的条件下比较：

- 随机 point-like hotspot；
- on-grid CW；
- off-grid CW；
- CW + isotropic GWB。

off-grid 频率写为

\[
f_0=(k+\delta)/T,
\qquad \delta=0,0.25,0.5,0.75.
\]

CW 空间模型使用单源相关指纹 \(\Upsilon_{ab}\)，频率模型使用实际采样窗产生的跨频响应。最终比较随机热点、CW 和混合模型。

## 4. 软件层次

- `simulation`：阵列、噪声、随机背景与 CW 注入。
- `response`：天线响应、ORF、HD、像素和球谐基底。
- `pfos`：逐频 pair correlations、误差和协方差。
- `skymap`：平方根球谐拟合和像素上限。
- `cw_fingerprint`：单源 \(\Upsilon_{ab}\) 与连续频率响应。
- `statistics`：null 校准、覆盖率、假阳性率和模型比较。
- `tests`：独立公式闭环和收敛测试。

当前 `demo/cw_intrinsic.py` 只作为确定性 CW 和有限窗响应的单元测试来源；旧 PFOS/radiometer 图不作为论文复现结果。

## 5. 验收门槛

### Gate A：前向模型

- 各向同性像素积分恢复 HD；
- 单像素响应与直接天线乘积一致；
- 单位、\(4\pi\)、\(\Delta\Omega\)、\(\Delta f\) 因子全部有测试；
- pair covariance 为半正定。

### Gate B：统计校准

- null 假阳性率与预注册阈值一致；
- 各向同性注入不产生系统性 \(C_{\ell>0}\)；
- 逐像素上限达到声明覆盖率；
- 不使用注入结果反调阈值。

### Gate C：论文复现

- 点源方向依赖和检测率在 Monte Carlo 误差内重现；
- \(C_1/C_0\simeq0.3\) 的结论由检测率区间支持；
- 所有图均由保存的数值表生成，而非只比较图形。

### Gate D：CW 判别

- 在独立测试集上报告 CW 正确识别率；
- 报告随机热点被误判为 CW 的概率；
- 比较只用单频 sky map、加入空间指纹、加入跨频信息三种情况；
- 检查结果对噪声失配、方向、频率和随机种子的稳健性。

## 6. 论文未明确锁定的项目

以下项目在取得正文精确信息或完成独立核对前不得静默假设：

- 48 个方向的像素索引；
- 列出的频率与精确 \(k/T\) 的冲突应采用哪一个；
- 点源加入后是否重新归一化全天总功率；
- 68 颗信号生成后 PFOS 是否实际保留 J0614-3329；
- 每个配置的 Monte Carlo 次数；
- 随机种子；
- CURN 和逐脉冲星噪声参数取值方式；
- 每对脉冲星、每个频段的完整 \(\sigma_{ab}(f_k)\)；论文只给出五个中位数；
- PFOS 是否采用最大似然噪声参数或噪声边缘化；
- 平方根球谐的实基底排列、幅度归一化和符号简并处理；
- LMFIT 的优化器、边界、初值和 multi-start 设置；
- 似然比“3 sigma”的单侧/双侧定义与 look-elsewhere 处理；
- 逐像素上限的精确定义和覆盖率目标。

其中 48 点索引、68/67 选择、完整 \(\sigma_{ab}\)、点源归一化及 3σ
定义直接影响图 4，编码时必须显式分支。随机种子、优化器初值和频率
微小差异可通过预注册的多种子/敏感性分析量化。

论文未说明的项目将采用预注册主方案，并通过敏感性分析处理；不得把自主选择标成“忠实复现”。

## 7. 实施顺序

1. 建立论文参数表和图表目标表。
2. 实现不依赖 `enterprise` 的 ORF/HD 最小闭环。
3. 与 `defiant` 的 PFOS 输出交叉验证。
4. 加入平方根球谐拟合和 null 校准。
5. 复现点源实验。
6. 加入 \(\Upsilon_{ab}\) 和 off-grid CW。
7. 最后再进入完整 TOA、现实采样和噪声失配测试。
