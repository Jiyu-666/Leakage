# Leakage 目录物理一致性审查报告

审查日期：2026-07-26  
审查对象：`/home/liujiyu/projects/Leakage` 下的 notebook、`cw_intrinsic.py`、PINT `par/tim` 数据、预计算 `npz`、归档和环境文件。

## 一、结论

**有条件地站得住脚。**

该目录能够支撑如下结论：

> 对一个无噪声、规则采样、只含相干 Earth term、圆轨道且不演化的单色连续引力波（CW），四振幅相干模板能够正确重构信号；有限观测窗会产生预期的频率泄漏，timing-model 投影和失配的 stochastic/radiometer 管线会进一步改变响应形状。

但它**不能**支撑以下更强结论：

- 真实 PTA 的泄漏核就是这里的曲线；
- 真实数据可以达到 notebook 中微角秒量级的定位/频率恢复；
- 当前 `post-fit` 结果代表完整脉冲星 timing fit；
- PFOS 或 radiometer 曲线是确定性 CW 的本征响应；
- 当前结果已经是可直接用于天体物理推断或灵敏度预报的端到端 PTA 模拟。

核心物理模型没有发现会使这个受限基准整体失效的一级错误。主要问题是**外推边界和可复现性**，不是核心天线响应、量纲或频率窗公式错误。

## 二、审查范围与方法

重点审查了：

- `demo/cw_intrinsic.py`
- `demo/notebook.cw-intrinsic-leakage.ipynb` 及其 executed 版本
- `demo/notebook.cw-batches.ipynb`、`demo/notebook.cw-batches.wsl.executed.ipynb`
- 旧版 `demo/notebook.ipynb`
- `demo/data`、`demo/cw_batch_data` 中的 `par/tim`、manifest、归档与 3 个 `npz`
- `environment.yml` 与 `environment.linux-64.lock.yml`

采用了以下核查：

1. 检查公式、符号约定、量纲、时间/频率单位及信号振幅标度。
2. 将本地实现与锁定提交中的 [`pta_replicator.add_cgw`](https://github.com/bencebecsy/pta_replicator/blob/a2147f7b268b8111290b3c98a9cd416a6e8eaf69/pta_replicator/deterministic.py) 对照。
3. 与公开 CW PTA 模型中的 Earth/pulsar term、天线响应和残差振幅公式对照，例如 [IPTA DR2 CW 搜索论文](https://doi.org/10.1093/mnras/stad812) 和 [Zhu 等关于 pulsar term 的研究](https://academic.oup.com/mnras/article/461/2/1317/2608555)。
4. 不依赖 NumPy，独立解析现有 `npz`，检查形状、有限性、峰值、条件数和跨文件一致性。
5. 重新计算应变、残差尺度、chirp 时间和观测期内频漂。
6. 重算 manifest 和归档中的 SHA-256。

当前 shell 没有安装项目的 Conda 环境（系统 Python 也没有 NumPy），因此没有从头重跑全部 notebook；本报告对已保存执行结果做了静态审查和独立数值复核。目录提供了环境快照，但见后文的依赖缺口。

## 三、模型与数值尺度

### 3.1 模拟配置

| 量 | 当前值 |
|---|---:|
| 脉冲星数 | 25 |
| 每颗 TOA 数 | 261 |
| 总样本数 | 6525 |
| 观测跨度 | 314,496,000.0047 s = 9.96578 yr |
| cadence | 1,209,600.00009 s ≈ 14 d |
| 单个 TOA 不确定度 | 0.1 μs = 100 ns |
| 基频 \(1/T\) | 3.17969068 nHz |
| 注入频段 | \(5/T\)–\(6/T\) = 15.898–19.078 nHz |
| 代表频率 | 17.4883 nHz，即 \(5.5/T\) |
| chirp mass | \(10^9\,M_\odot\) |
| luminosity distance | 100 Mpc |
| 源位置 | RA \(2^\circ\)，Dec \(0.4^\circ\) |
| 信号 | Earth term only，`psrTerm=False` |
| 演化 | `evolve=False` |

这是一个刻意理想化的算法基准：25 颗脉冲星近似 Fibonacci 均匀铺天，采样和误差几乎完全相同，所有脉冲星使用相同的简单自转模型，没有真实观测中的缺测、异方差、红噪声、DM 噪声、后端跳变等。

### 3.2 振幅与量纲

对代表频率 \(f=17.4883\) nHz，采用

\[
h_0=
\frac{2(G\mathcal M_c)^{5/3}(\pi f)^{2/3}}{c^4D_L}
\]

得到

\[
h_0 \simeq 4.00\times10^{-15}.
\]

对应的基本 timing-residual 尺度

\[
\frac{h_0}{2\pi f}\simeq 36.44\ {\rm ns}.
\]

notebook 中 J00 的半峰峰值为 29.30 ns，canonical CW 数据集全阵列最大绝对残差为 42.03 ns；考虑天线方向图、倾角和偏振后，这与 36.44 ns 的基本尺度一致。

`add_cgw` 中的

\[
\alpha=\mathcal M_c^{5/3}D_L^{-1}(\pi f)^{-1/3}
\]

量纲为时间；`cw_intrinsic.py` 的天线因子无量纲、模板列无量纲、拟合振幅为秒，\(2\mathcal F=\Delta\chi^2\) 无量纲。未发现秒/天、Hz/nHz、Mpc/kpc 或角度单位混用。

需要在将来写成论文时明确：这里的 \(\mathcal M_c\) 应按探测器框架的红移 chirp mass 解释；100 Mpc 处红移虽小，但“源框架质量”和“红移质量”不应混写。

### 3.3 单色近似

按四极辐射公式重新计算，代表点的合并前剩余时间约为 \(9.95\times10^3\) yr，观测期内

\[
\dot f\,T \simeq 6.57\times10^{-12}\ {\rm Hz},
\qquad
\frac{\dot f\,T}{1/T}\simeq 2.07\times10^{-3}.
\]

即 10 年内只漂移约 0.002 个 Fourier bin，累计二次相位约 0.001 周期。因此，对当前 **Earth-term-only、10 年** 基准，`evolve=False` 是合理近似。

若以后打开 pulsar term，信号要回溯约千年，不能自动沿用这一判断；应加入脉冲星距离和 Earth/pulsar term 的频率演化。Earth term 的相干性和 pulsar term 对定位的影响已有明确文献依据。100 Mpc 也接近需要留意 PTA 波前曲率的尺度，完整源模型可参考 [Deng & Finn 2011](https://arxiv.org/abs/1008.0320)。

## 四、通过的物理与数值检查

### 4.1 天线响应和四振幅模型正确

`source_basis`、\(F_+\)、\(F_\times\) 的传播方向和分母 \(1+\hat\Omega\cdot\hat p\) 与锁定版本的 `pta_replicator.add_cgw` 一致。四列

\[
(-F_+\sin\Phi,\,-F_+\cos\Phi,\,-F_\times\sin\Phi,\,-F_\times\cos\Phi)
\]

确实张成固定天空、固定频率的任意 Earth-term 单色偏振信号。

4 组偏振/倾角/初相位的相对重构误差均约 \(3.0\times10^{-14}\)，法矩阵条件数约 1.63；整个已知天空扫描中的条件数为 1.615–2.090，没有数值奇异性。

### 4.2 有限窗实现正确

对单颗脉冲星，直接 DFT、cos/sin 设计矩阵和包含正负频率分量的精确采样窗预测相符：

- 单色重构最大相对误差：\(3.31\times10^{-14}\)
- 直接功率与精确窗预测最大相对误差：\(4.02\times10^{-15}\)

Fejér/Dirichlet 有限样本公式及零频差极限实现正确。代码还正确保留了实信号的负频率镜像项。

但“raw 等于采样窗”不宜按字面理解：独立比较显示，归一化 coherent raw \(2\mathcal F\) 与单颗脉冲星精确 DFT 窗的最大逐点差约 0.0506。原因是 coherent \(2\mathcal F\) 同时拟合两种时间正交分量并用随试探频率变化的法矩阵归一化。它是**由有限采样窗产生的相干匹配滤波响应**，不是单纯的 DFT/Fejér 功率核。notebook 末尾“raw 是最接近本征窗响应的量”的说法比开头“raw 给出纯采样窗响应”更准确。

### 4.3 振幅随频率的标度正确

固定 \(\mathcal M_c,D_L\) 时，\(h_0\propto f^{2/3}\)，残差振幅 \(\propto f^{-1/3}\)，匹配功率应 \(\propto f^{-2/3}\)。现有结果：

\[
\frac{P_6}{P_5}=0.8855488076556,\qquad
\left(\frac65\right)^{-2/3}=0.8855488076522,
\]

相对差 \(3.9\times10^{-12}\)。这很好地验证了注入和模板的单位/振幅约定，但属于同一模型内的实现自检，不是独立的天体物理测量。

### 4.4 信号功率与 \(2\mathcal F\) 一致

canonical \(k=5.5\) CW 数据集的全阵列残差 RMS 为 16.548 ns。用 6525 个样本和 100 ns 白噪声尺度直接估计：

\[
\sum r_i^2/\sigma^2 \simeq 178.68.
\]

缓存中的匹配峰值为 \(2\mathcal F=179.146\)，即名义网络 SNR \(\sqrt{2\mathcal F}=13.38\)。两者吻合，差别可由微小的 ideal-baseline 残差、均值分量和精确模板投影解释。

### 4.5 timing 投影的当前实现自洽

25 颗脉冲星的 active PINT design rank 全部为 1，唯一列是 `Offset`：

- 投影幂等误差：\(7.14\times10^{-15}\)
- 与被投影空间的正交误差：\(4.48\times10^{-16}\)
- 与显式去均值的相对差：\(3.05\times10^{-16}\)

数据和模板施加同一投影是正确做法。问题不在数学实现，而在当前 timing model 过于简单，见下一节。

### 4.6 数据完整性良好

- canonical ideal 的 50 个 `par/tim` 文件全部匹配 manifest SHA-256。
- canonical CW 的 50 个 `par/tim` 文件全部匹配 manifest SHA-256。
- CW manifest 记录的 parent ideal manifest 哈希与实际文件一致。
- legacy archive 的 50 个校验和全部通过，`demo/data/cw_partim` 与其逐文件一致。
- 两个 delta-scan `npz` 在共同的 \(k=4,5,6,7\) 区间内逐元素完全一致。
- 扩展 delta-scan 的主要数组全部为有限值，误差数组为正。
- intrinsic cache 的配置值、注入频率和 trial frequency 与当前 notebook 常量一致。

## 五、主要问题与风险

### B1：`post-fit` 只去除了常数偏置，不能代表真实 timing fit

当前 `par` 文件中 F0、位置等参数没有作为 active fit parameter，实际投影仅为 `Offset`。真实 PTA 通常至少会拟合相位、F0、F1、天体测量参数，很多源还涉及双星和 DM 参数；这些拟合会显著改变低频 transfer function 和泄漏。

**影响：** 当前 post-fit 曲线只说明“去均值后的变化”，不能外推到标准 timing analysis。notebook 已在边界说明中承认这一点，这是正确的，但所有图题和后续引用也应保留该限定。

### B2：无噪声同模型恢复不能当作定位精度

全天搜索用生成信号的同一四振幅 Earth-term 模型，在无随机噪声数据上进行连续优化。得到最大频率误差 \(5.46\times10^{-8}/T\)、最大天空误差 \(3.62\times10^{-6}\) deg，只说明目标函数和优化器可以回到注入点。

**影响：** 这不是统计定位误差，更不是 SNR≈13 的真实 PTA 可达到的角分辨率。应将相关表述改为“参数恢复/优化器闭环误差”，不要称作 localization accuracy。真实结论需要白噪声和红噪声 realization、重复 Monte Carlo、置信区域和 sky trials 校正。

### B3：PFOS/radiometer 是有意失配的诊断，不是 CW 本征核

执行 notebook 使用：

- HD PFOS stochastic template；
- 固定 \(\gamma=13/3\)、`gw_log10_A=-14` 的 common Fourier covariance；
- 非偏振 point-source radiometer；
- 确定性、固定偏振 Earth-term CW 注入。

模板与信号的物理假设不一致，所以 PFOS、source map 和 peak map 的频率形状不必等于 Fejér 或 coherent CW 核，负值也不违反物理。当前 notebook 已明确只比较归一化形状，这一解释是正确的。

**影响：** 这些结果可用于研究“确定性 CW 被 stochastic 管线如何解释”，不能命名为 CW 本征泄漏。

### B4：执行产物与可复现源存在断层

- `notebook.cw-batches.ipynb` 只有 19 个 cell；
- `notebook.cw-batches.wsl.executed.ipynb` 有 53 个 cell，额外包含 PFOS、MAPS 和完整 delta scan；
- 执行 notebook 导入 `defiant` 和 `maps`，但二者不在 `environment.yml` 或 Linux lock 中，也没有记录版本/提交；
- 当前简化 batch notebook 只按文件数量复用已有目录，不验证 manifest 配置和校验和；更可靠的不可变数据集逻辑反而只保留在 `archive/notebooks/notebook.cw-batches.verbose.ipynb`。

**影响：** stochastic/radiometer 结果目前不能从“canonical source + 声明环境”可靠重建。论文级使用前必须合并 canonical source、固定 DEFIANT/MAPS 版本，并恢复 manifest 验证。

### B5：intrinsic cache 验证不足

`cw_matched_scan_v1.npz` 存在时只检查 `format_version == 1`，没有核对 \(T\)、cadence、源位置、\(\sigma\)、频率网格、偏振集合、ideal manifest 哈希或 `cw_intrinsic.py` 版本。

当前缓存经本次审查与当前配置一致，所以**现有结论未因此失效**；但以后只要修改常量而忘记删缓存，就可能把旧数组按新配置绘图和保存。

### C1：数据仍有小的数值基线残差

ideal manifest 的残差 RMS 为 0.125 ns、最大值 0.320 ns；J00 注入后 PINT residual 与登记的 CW waveform 相差 RMS 0.301 ns、最大 2.46 ns。它们远小于 100 ns TOA 不确定度，也不影响当前 leakage 结论，但说明 “ideal=严格零”和 “PINT residual=逐位等于注入波形”都不成立。

### C2：模型参数极度理想化

所有脉冲星具有同样的 F0，PM、PX 为零，没有 F1/DM/距离，采样规则且同频，天空分布近均匀。enterprise 关于缺少脉冲星距离而采用 1 kpc 默认值的警告，在 `psrTerm=False` 的当前流程中无实质影响；若打开 pulsar term 则必须修正。

### C3：少量数值鲁棒性边界

天线函数在 \(|1+\hat\Omega\cdot\hat p|<10^{-12}\) 时直接置零，这是处理 \(0/0\) 方向极限的数值约定，不是严格物理极限；当前真实源与最近脉冲星相距 11.52°，最小分母约 0.0202，因此没有触发。Nelder–Mead 精化也没有在目标函数内部严格限制 \(\theta\)，当前所有保存结果均成功且回到真值，但更一般扫描宜使用有界参数化。

## 六、建议的修改顺序

1. **先统一可复现源。** 将 53-cell 执行 notebook 的科学代码整理为 canonical source，补齐并固定 `defiant`、`maps` 版本；把 archived manifest/checksum 逻辑恢复到活跃 batch notebook。
2. **为缓存加入完整签名。** 至少校验 ideal manifest SHA-256、源位置、\(T\)、cadence、\(\sigma\)、所有网格、偏振参数、代码版本；不一致时拒绝加载。
3. **纠正图和文字的物理名称。** 使用“coherent finite-window response”“optimizer closure error”“stochastic-template response”，避免“纯 Fejér 核”“定位精度”等过强表述。
4. **增加真实 timing fit 分支。** 激活 Offset、F0、F1 和基本天体测量参数，使用一般化白噪声加权投影，再比较 transfer function。
5. **增加统计验证。** 注入异方差白噪声、红噪声/DM 噪声，做多 realization Monte Carlo；报告偏差、覆盖率、置信区间和 sky trials。
6. **再增加完整源物理。** 加入可靠脉冲星距离、pulsar term 和适当的频率演化；若要把 100 Mpc 源解释为真实系统，还应明确红移质量及是否需要波前曲率修正。

## 七、最终判定

| 用途 | 判定 |
|---|---|
| 教学/算法自检：有限窗如何让单色 Earth-term CW 泄漏 | **通过** |
| 四振幅 coherent template 与 `add_cgw` 的闭环验证 | **通过** |
| 说明 timing 去均值和失配 stochastic 管线会改变响应 | **通过，但必须保留限定** |
| 完整 timing-fit 后的真实 PTA leakage kernel | **未验证** |
| 真实 PTA 的检测率、定位精度或频率误差 | **不通过，现有实验不能支撑** |
| 可由当前 canonical source + 环境文件完整复现全部图 | **不通过，需要补齐源和依赖** |

最准确的一句话总结是：

> **这里的核心物理和数值实现足以作为受控的 Earth-term CW 有限窗基准；它还不是现实 PTA 观测或天体物理推断模型。**
